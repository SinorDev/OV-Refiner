# OV-Refiner
OV Refiner - Domain to IP Resolver for OVPN &amp; V2Ray Configs. It's a specialized tool developed in C# that can resolving domain endpoints via DoH, and generating IP-based V2Ray &amp; OVPN configuration files to bypass DNS Hijacking or DNS Spoofing.
